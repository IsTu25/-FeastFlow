apiVersion: v1
kind: Secret
metadata:
  name: feastflow-secrets
  namespace: default
type: Opaque
stringData:
  # ⚠️ Replace these values before deploying to production.
  # Use Sealed Secrets (https://github.com/bitnami-labs/sealed-secrets) or
  # HashiCorp Vault for secure secret management in real environments.
  REDIS_URL: "redis://redis:6379"
  JWT_SECRET: "changeme-in-production-use-32-chars-minimum"
  DB_PASSWORD: "changeme-in-production"
  DB_URL: "postgresql://iut_user:changeme-in-production@postgres:5432/iut_cafeteria"
